/*
 * ClientSignup Messages
 *
 * This contains all the text for the ClientSignup container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ClientSignup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ClientSignup container!',
  },
});
