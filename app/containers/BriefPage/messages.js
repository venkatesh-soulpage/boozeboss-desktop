/*
 * BriefPage Messages
 *
 * This contains all the text for the BriefPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BriefPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BriefPage container!',
  },
});
