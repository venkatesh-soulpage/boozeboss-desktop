/*
 * ClientContainer Messages
 *
 * This contains all the text for the ClientContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ClientContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ClientContainer container!',
  },
});
