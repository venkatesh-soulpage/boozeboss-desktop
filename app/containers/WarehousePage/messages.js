/*
 * Warehouse Messages
 *
 * This contains all the text for the Warehouse container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Warehouse';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Warehouse container!',
  },
});
