/*
 * OrganizationSignup Messages
 *
 * This contains all the text for the OrganizationSignup container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrganizationSignup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrganizationSignup container!',
  },
});
