/*
 * OrganizationsPage Messages
 *
 * This contains all the text for the OrganizationsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrganizationsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrganizationsPage container!',
  },
});
